# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CurrencyUI' do
	pod 'SwiftyJSON', '~> 4.0'
	pod 'Alamofire', '~> 4.0.0'
	pod 'SnapKit', '~> 4.0.0'
    pod 'Charts'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CurrencyUI

  target 'CurrencyUIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
